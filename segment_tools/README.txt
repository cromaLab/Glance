runAllTestsAPI.rb <txt file of session names>
* CSVs. Set output dir with global var.

apiResults.rb <session> <ToolName> <mode> [betterJSON]
* JSON matrix of data

getFastestTimeRabbit.php?session=<session>&oldClips=<bool>
* time until first clicked.
* oldClips is true for rabbitHandGestureXX, not needed for everything else 